import { Box, Typography } from "@mui/material";

export default function AdminRoomsPage() {
  return (
    <Box>
      <Typography variant="body1" color="text.secondary">
        Rooms management page (table + search + drawer will be here).
      </Typography>
    </Box>
  );
}
