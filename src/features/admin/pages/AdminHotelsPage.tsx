import { Box, Typography } from "@mui/material";

export default function AdminHotelsPage() {
  return (
    <Box>
      <Typography variant="body1" color="text.secondary">
        Hotels management page (table + search + drawer will be here).
      </Typography>
    </Box>
  );
}
